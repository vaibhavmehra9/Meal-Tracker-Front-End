import styled from "styled-components";

const MealItemStyle = styled.div`
  padding: 10px;
  background: rgb(191 207 255 / 0.3);
  margin: 0 0 20px 0;
  border-radius: 5px;
  border: 5px solid var(--light-blue);
`;

export default MealItemStyle;
